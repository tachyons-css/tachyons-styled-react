import styled from "@emotion/styled"
import { space, width, display, fontSize, color, textStyle } from "styled-system"

const Em = styled.em(space, width, display, fontSize, color, textStyle, {})

Em.defaultProps = {}

export default Em
